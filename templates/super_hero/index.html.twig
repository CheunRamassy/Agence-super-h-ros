{% extends 'base.html.twig' %}

{% block title %}Hello SuperHeroController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

{# <table>
  <tr>
    <td>Nom</td>
    <td>Tobias</td>
    <td>Linus</td>
  </tr>
  <tr>
    <td>16</td>
    <td>14</td>
    <td>10</td>
  </tr>
</table> #}
<div class="container-xl">
  <h1 class="display-3">Les supers héros de l'agence</h1>
</div>

<div class="container-xl">
  <div class="d-flex flex-row-reverse">
  <a class="btn btn-primary" href= "{{ path('create_hero') }}"type="button" >Ajouter un héros</a>
  </div>
</div>



{# <div class="container-xl"> #}
  {# <div class="container-flex-col"> #}

      {% for hero in heros %}
          
        <div class="container-xl">
 {# <div class="grid text-center" style="row-gap: 0;"> #}
        {# <div class="container-flex-col"> #}
                <div class="card">
                  <a href= "{{ path('edit_hero', {id: hero.id}) }}">
                    <p class="time-text"><span>11:11</span><span class="time-sub-text">{{hero.name}}</span></p>
                    <p class="day-text">    
                        {{hero.alterEgo}}
                        {{hero.available}}
                        {{hero.energyLevel}}
                        {{hero.biography}}
                        {{hero.createdAt| date}}
                        </p>
                  
                    <a href= "{{ path('remove_hero', {id: hero.id}) }}"><img src="x.png"></a>
                  </a>
                </div>
          </div>
              {% endfor %}
    {# </div> #}
  </div>
</div>

{# </div> #}
    {# <div class="container-xl">

    <div class="container-xl"> #}
        {# <div class="container-bloc"> #}

      {# </div> #}
    {# </div> #}

</div>
{% endblock %}
